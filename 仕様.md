# オンライン大喜利アプリ 仕様書 v2

v2（2026-09-25）: ステージ画面を1人ずつ発表する形式に変更
v2.1（2026-09-25）: MC自身も回答できる（「自分の回答」）、参加者の並び順をドラッグで変更

友達数人（2〜10人）とDiscord通話しながら遊ぶ、ブラウザだけで動く大喜利アプリ。
サーバー不要。ホスト（MC）のブラウザが「部屋の親」になり、参加者はWebRTC（PeerJS）で直接つながる。

## 決定事項

| 項目 | 決定 |
|---|---|
| 接続方式 | PeerJS（WebRTC）。ホストのブラウザが状態を持つスター型。サーバー不要、静的ファイルのみ（GitHub Pagesに置くだけで動く） |
| 端末 | PC（マウス／ペンタブ）とスマホ／タブレット両対応。スマホ縦画面レイアウトあり |
| 配信用 | OBSブラウザソースで取り込む「ステージ画面」あり（1920×1080想定） |
| SE | ホストのみ鳴らせる。内蔵SE（Web Audioで合成、音源ファイル不要）＋自前mp3の追加可 |
| 技術 | ビルド不要のバニラJS（ES modules）、HTML/CSS/JS。外部依存はPeerJSのCDNのみ |
| 言語 | UIはすべて日本語 |

## 3つの画面（役割）

1つの `index.html` で、トップ画面から役割を選ぶ（またはURLパラメータで直行）。

### 1. トップ
- タイトル「オンライン大喜利」
- 「部屋を作る（MC）」「参加する」「ステージ画面（配信用）」の3ボタン
- 参加する: 部屋コード（4文字）と名前を入力して入室。前回の名前は記憶しておく
- URLパラメータ: `?room=XXXX` で参加画面にコードを入れた状態で開く。`?room=XXXX&stage=1` でステージ画面に直行。`&chroma=1` でステージ背景を緑（クロマキー用）、`&mute=1` でステージのSE無音

### 2. MC画面（ホスト）
- ヘッダ: 部屋コードを大きく表示、その横にMCの名前（クリックで変更。localStorage `ogiri.hostName`、デフォルト「MC」）、「参加用URLをコピー」「ステージURLをコピー」ボタン、接続人数（MC自身は数えない）
- お題エリア
  - テキスト入力＋「出題」ボタン（ステージと全員の画面に即反映）
  - お題リスト: 1行1お題のテキストエリア（localStorageに保存）。「ランダム出題」でリストから未出題のものを1つ選んで出題。全部出したら最初から
  - チェックボックス「出題時にリセット」（デフォルトON）: ONなら出題時に全員のフリップを空・未提出に戻し、挙手リセット、ステージを下げ、回答履歴も消す
- 自分の回答（お題エリアの下、参加者一覧の上）: MCが交代せずに自分のフリップで回答する
  - 見出しの右にチェックボックス「自分も回答する」（`hostAnswers`、デフォルトON）。OFFにすると中身を折りたたみ、参加者一覧・ステージからMCが消える（フリップの内容は保持し、ONで戻る。OFFの間に発表中だったらステージは下がる）
  - 中身は参加者画面と同じフリップ編集（手書き／文字、ペン色・太さ・消しゴム・戻す・全消し、文字テキストエリア）と「提出する／書き直す」ボタン。実装は `js/flipEditor.js` を参加者画面と共用
  - MCのフリップはメッセージを使わずホスト内で直接 state を更新する（`setFlip('host', flip)`／`setSubmitted('host', bool)`）。送るタイミングは参加者と同じ（pointerup ごと／文字は300msデバウンス）
  - 自分が発表中（`stage.playerId === 'host'`）: 編集ロック、フリップの上に「あなたの番です！」と「フリップをオープン！」（押すと `openStage('host')`）。オープン後はバッジ「オープン中」と「「下げる」と次の回答を書けます」
  - 「下げる」の履歴追加・白紙化、「全部クリア」「出題時にリセット」は参加者と同じようにMCにも効く（「自分も回答する」OFFでもMCのフリップは空になる）
- 参加者一覧（カード形式）
  - MC自身のカードも並ぶ（「MC」バッジ付き。ライブプレビュー、ステータス、「ステージへ」「下げる」「オープン（MC側で）」。「退室させる」「MCを渡す」は出さない）
  - 並び替え: カード左上の「⠿」をドラッグしてカードの順番を入れ替える（Pointer Events、マウス・ペン・タッチ共通。⠿ から始めたドラッグだけが並び替えになり、カード内のボタンの操作は邪魔しない）。ドラッグ中はつかんだカードが半透明で追従し、挿入位置にアクセント色の細い縦線が出る。画面の上下端に寄せると自動スクロール。離した位置で `state.reorder(newOrder)`。ドラッグ中は一覧の再描画を保留し、終わってから反映する。見出しの横に「⠿ をドラッグで並び替え（ステージの並びも変わります）」
  - 並び順はステージ下部の一列と同じ（挙手の順番とは無関係）
  - 各カードに: 名前、接続状態（切断中はグレー）、フリップのライブプレビュー（書いている途中も見える）、ステータスバッジ（待機中／記入中／提出済み／発表中／切断）、挙手中なら✋と順番
  - カードのボタン
    - 通常時: 「ステージへ」「退室させる」「MCを渡す」（控えめな見た目）
    - その人が発表中（未オープン）: 「オープン（MC側で）」（本人が押せないときの保険）「下げる」
    - オープン済み: 「下げる」
    - 「MCを渡す」は切断中・発表中の人には押せない（disabled）。詳しくは「MCの交代」節
  - 発表中の人のカードは枠を強調
  - 「ステージへ」: その人をステージ中央に呼ぶ。その人の挙手は下ろす（他の人の順番は詰める）。既に誰か出ていれば入れ替え（前の人は履歴に入れず、そのまま戻す）
  - 「下げる」: ステージから下げ、その回答を「回答履歴」に移す。その人のフリップは白紙・未提出に戻る（参加者のキャンバスも白紙になる）
- 全体操作ボタン: 「全部クリア（次の回へ）」（全員のフリップを空に・挙手リセット・ステージを下げる。履歴は残す）「挙手リセット」
- 挙手順リスト（挙げた順に名前を並べる）。各行に「ステージへ」ボタン
- 回答履歴（このお題）: 下げた回答を新しい順に、名前・小さなフリップ・時刻（HH:MM）で一覧。件数表示と「履歴を消す」ボタン。お題を変える（リセットON）と消える。ホスト内にだけ持ち、参加者・ステージへは送らない
- シンキングタイマー: 秒数（デフォルト60）を入れて「スタート」「停止」。ステージと参加者画面に残り秒を表示。0になったら内蔵SE「ジャン」を自動再生（チェックボックスでOFF可）
- SEパッド
  - 内蔵SEボタン: ドン（太鼓一発）／ドラムロール／ジャン（時間切れ）／ピンポン（正解）／ブザー（不正解）／拍手／チーン
  - チェックボックス「オープン時に自動でドンを鳴らす」（デフォルトON、localStorage保存）: 発表中のフリップがオープンされた瞬間に全員へ `se {id:'don'}` を送る
  - 「mp3を追加」ボタン: ファイルを選ぶとボタンが増える。名前は編集可。IndexedDBに保存して次回も残る。ボタン長押し（または×ボタン）で削除
  - 音量スライダー、ミュートトグル
  - SEはホスト画面・ステージ画面・参加者画面すべてで再生する（各画面にミュートあり）。自前mp3は1.5MB以下なら接続時と追加時に全員へ転送して全員で鳴る。超える場合はホストとステージ（同じPC）でのみ鳴らし、追加時に警告を出す

### 3. 参加者画面
- 上部にお題を大きく表示。接続状態（接続中／再接続中…）
- フリップ（4:3、論理座標 800×600、画面幅に合わせて拡縮）
  - モード切替: 「手書き」「文字」
  - 手書き: ペン色（黒／赤／青）、太さ3段階、消しゴム、戻す（1ストローク）、全消し（確認なしで即消える。「戻す」で全消し前の状態に戻せる）。Pointer Eventsで実装、`touch-action: none`、スマホでスクロールしないこと。線はなめらかに（二次ベジェで中点補間）
  - 文字: テキストエリアに入力。フリップ上では改行を反映し、はみ出さないよう自動でフォントサイズを縮める（最大4行程度を想定）
- ボタン: 「提出する」（未提出→提出済）、「書き直す」（提出済→未提出。内容は残す）、「✋ 挙手」（トグル。もう一度押すと取り下げ）
- 自分が発表中（MCに「ステージへ」呼ばれた）とき
  - 未オープン: フリップの上に大きく「あなたの番です！」と「フリップをオープン！」ボタン。押すと `open` を送り、ステージでフリップがめくれる。フリップの編集・提出・挙手はロック
  - オープン済み: バッジ「オープン中」。編集ロックのまま「MCが下げると次の回答を書けます」と案内
  - MCに下げられたら自動で通常状態に戻る（キャンバスは白紙、ボタンは「提出する」）
- MCが「全部クリア」または（リセットONで）出題したら自分のフリップも空になる
- 名前の変更（ヘッダの名前をタップで編集）
- スマホ縦: フリップは幅いっぱい、ツールバーは下部に固定、ボタンは指で押しやすい大きさ（44px以上）
- 書いた内容はストロークが終わるたび（pointerup）／文字入力は300msデバウンスでホストへ送る

### 4. ステージ画面（配信用）
- 背景: ダーク（デフォルト）。`?chroma=1` なら `#00FF00` 単色
- 上部にお題を大きく（フォント約56px、長ければ自動縮小）
- 中央: MCが呼んだ1人のフリップだけを大きく表示（4:3、高さ約58vh）。誰も出ていないときは何も出さない（薄い装飾のみ）
  - 登場: scale(0.6)→1 ＋フェード（0.4秒）
  - 未オープン: 裏面に大きく「〇〇 さんの回答」（MCなら名前の横に小さく「MC」）
  - オープン: めくりアニメ（CSS 3D transform rotateY、0.6秒）で内容表示。カードの下に名前
  - 下げられたら: フェードアウトしながら少し下へ（0.3秒）消える。別の人に入れ替えられたときは前のカードを即消し
- 下部: 回答者ステータスの横一列。受け取った `players` の順（＝MCが決めた並び順）に並べる。MCが回答するときはMCも並ぶ（名前の横に小さく「MC」）。並び替えられたときは要素を作り直さずに移動し、transform の transition（0.4秒）で滑らかに位置が変わる。人数に応じて幅を縮め必ず1行（最大10人想定）。各項目は名前＋ステータス（待機中: 灰／記入中: 白／提出済み: アクセント色／発表中: 反転／切断: 暗く半透明）。挙手中は右上に✋と順番（揺れるアニメ）。フリップの中身は出さない
- タイマー: 右上に残り秒を大きく。残り10秒以下で赤
- SEはステージでも再生（`?mute=1`で無音）
- 1920×1080で余白バランスよく。フォントは大きめ

## 通信仕様（PeerJS）

- CDN: `https://cdn.jsdelivr.net/npm/peerjs@1.5.4/dist/peerjs.min.js`
- ホストのPeer ID: `ogiri-` + 部屋コード4文字。部屋コードは英大文字＋数字から、見間違えやすい `0 O 1 I` を除いてランダム生成。ホストは部屋コードを `sessionStorage` に保存し、リロード時は同じIDで再作成する（参加者が自動再接続できるように）。IDが取れなかった（既に使われている）場合は新しいコードで再試行
- ICEサーバー: Google STUN（`stun:stun.l.google.com:19302`）＋無料TURN（`turn:openrelay.metered.ca:80` / `turn:openrelay.metered.ca:443`、ユーザー名・パスワードともに `openrelayproject`）
- 参加者・ステージ: `peer.connect(hostId, { reliable: true })` で接続し、開通後に `hello` を送る
- 参加者は `clientId`（UUID）を `localStorage` に持ち、再接続時に同じIDを送る。ホストは同じ `clientId` なら同一プレイヤーとして復帰させ、フリップ内容を保持する
- 切断したら参加者側は3秒ごとに自動再接続を試みる（最大無制限、画面に「再接続中…」）
- ホストは接続が切れたプレイヤーを `connected: false` として残す（内容は保持）。「退室させる」で完全削除
- `clientId` が `'host'`（MC自身用に予約）で来た場合、ホストはランダムなIDに置き換える

### メッセージ（JSON、`t` がタイプ）

クライアント → ホスト
- `hello { role: "player" | "stage", name, clientId }`
- `flip { mode: "draw" | "text", strokes, text }`
- `submit { submitted: boolean }`
- `hand { raised: boolean }`
- `rename { name }`
- `open {}` 発表中の本人がフリップをめくる。ホストは stage.playerId が送信者本人のときだけ受理して `stage.opened = true`
- `handoverAck {}` `handover` を受け取った参加者が返す（MCの交代）

ホスト → クライアント
- `state { ... }` 状態の全体スナップショット。状態が変わるたびに50msデバウンスで全員へ送る
- `se { id }` SE再生指示（内蔵ID または カスタムID）
- `seCustom { id, name, mime, data }` 自前mp3の本体（ArrayBuffer）。接続直後と追加時に送る
- `kicked {}` 退室させられた
- `hostChanging { newHostName }` MCが交代する予告。新しいMC以外の参加者・ステージへ送る
- `handover { code, snapshot, topicList, customSE: [{ id, name, mime }], usedTopics, now }` 新しいMCになる参加者へ送る
  - `snapshot`: ホスト状態の完全な形（sessionStorage 保存と同じ `toSave()` の形。players のフリップ・rev・挙手、topic、history、stage、timer、round、seq、hostAnswers、order など）。ただし MC自身の回答（`host`）は除き、stage が `'host'` なら null にして送る
  - `topicList`: 前のMCのお題リスト（テキストエリアの文字列）
  - `usedTopics`: ランダム出題の出題済み一覧、`now`: 送信時のホストの時計（タイマーの時刻合わせ用）

### state の形

```
{
  room: "AB7K",
  topic: "こんな大喜利アプリは嫌だ",
  players: [                                  // order の順に並べて送る（受け側はこの順で表示するだけ）
    { id, name, connected, submitted,
      isHost?: true,                          // MC自身（id は 'host'）のときだけ付く
      hand: { raised: true, order: 1 } | { raised: false },
      flip: { mode: "draw", strokes: [...], text: "" },
      rev }                                   // flip が変わるたびに増える
  ],
  stage: null | { playerId, opened: boolean, since: <epochミリ秒> },   // 中央に出ている人
  timer: { endsAt: <epochミリ秒> | null, total: 60 },
  round: 3,                                   // 全員クリアのたびに増える
  now: <epochミリ秒>,                         // ホストの時計
  seq: 123
}
```

ホスト内部だけに持つもの（クライアントへは送らない。sessionStorage に保存してリロードで復元）:

```
history: [ { id, name, flip, at: <epochミリ秒> } ]   // 現在のお題で発表済み（下げた）回答
hostAnswers: true                                    // 「自分も回答する」。false なら players から 'host' を外す
order: ["host", "<clientId>", ...]                   // 並び順。'host' も含む（初期位置は先頭、OFFの間も位置を保持）
```

- MC自身のエントリ: `{ id: 'host', isHost: true, name: <MCの名前>, connected: true（常に）, hand: { raised: false }（挙手しない）, submitted, flip, rev }`。`hostAnswers` が true のときだけ `players` に入る。`playerStatus()` はそのまま使える
- 並び順: 新規参加者は `order` の末尾に追加、「退室させる」で除去。`reorder(ids)` に含まれない id（OFF中の 'host' など）は元の位置を保つ。挙手の順番（hand.order）は挙げた順のままで並び順とは無関係
- `toSave()` の形: `players` には参加者だけ、MC自身は `host: { name, submitted, rev, flip }` に分けて持つ。ほかに `hostAnswers`、`order`

各プレイヤーの表示ステータスは受け側で導出する（`state.js` の `playerStatus()` を MC・ステージで共用）:
`off`（connected=false）→ `onstage`（stage.playerId が本人）→ `submitted`（提出済み）→ `writing`（未提出で flip に内容あり）→ `idle`（それ以外）の順に判定。挙手はこれとは独立に ✋＋順番 で表示。

### ストロークの形

```
{ color: "#000", width: 6, erase: false, pts: [[x, y], ...] }   // 論理座標 0..800 × 0..600 の整数
```

## MCの交代

同じ部屋コードのまま、MC（ホスト）を参加者の1人に交代する。「ホストのブラウザが部屋の親」という構造は変えず、
新しいMCのブラウザが同じ Peer ID（`ogiri-` + 部屋コード）でホストとして立ち上がり直す。
画面内でモードを切り替えるのではなく、**sessionStorage に状態を書いてページ遷移し、既存の「ホストのリロード復元」で起動する**。

1. MC（A）が参加者カードの「MCを渡す」を押す（`confirm`「〇〇さんにMCを渡しますか？ あなたは参加者として入り直せます」）。接続中かつ発表中でない人だけ
2. A は B 以外の全員（参加者・ステージ）へ `hostChanging { newHostName }` を送る。受け取った画面は接続状態を「MC交代中…（〇〇さんへ）」にし、新しいホストにつながるまでその表示のまま（自動再接続は既存どおり3秒ごと）
3. A は B へ `handover` を送り、全画面に「〇〇さんにMCを渡しています…」を出す。以降 A は `handoverAck` 以外のメッセージを無視する
4. B は `handover` を受けたら
   - すぐ `handoverAck {}` を返し、全画面に「MCを引き継ぎます…」を出す
   - snapshot から自分（自分の clientId）を players から除外。挙手の順番を詰め、stage が自分なら null にする。並び順（order）は前のMCの 'host' を外し、自分の clientId があった位置を 'host'（自分の回答）に置き換える。タイマーの終了時刻は `now` との差で自分の時計に合わせる
   - sessionStorage に `ogiri.hostRoom`（部屋コード）・`ogiri.hostState`（状態）・`ogiri.usedTopics`・`ogiri.handoverPending = "1"` を保存し、`ogiri.playerRoom` を消す
   - localStorage のお題リストを「A の行 → 自分の行」の順でマージ（重複除去）して保存
   - 受信済みのカスタムSE（`seCustom` で届いたもの）を IndexedDB（`ogiri-se`）とメタ情報に登録。同じ id が既にあれば上書きしない。1.5MB超で届いていないものは引き継がれない
   - ack 送信から 300ms 以上待って `location.replace('?host=1')`
5. A は `handoverAck` を受けたら（3秒来なければタイムアウトで続行）すべての接続を閉じ、Peer を `destroy()`（Peer ID を解放）、sessionStorage のホスト状態・部屋コード・出題済みを消して `location.replace('?room=CODE')`。トップ画面の参加フォームに部屋コードと前回の名前が入り、「MCを〇〇さんに渡しました…」のお知らせが出る
6. B の新ホスト起動: `ogiri.handoverPending` があるときは `unavailable-id` 時の同一コード再試行を 8 回（2.5秒間隔）に増やす。起動中は MC 画面ヘッダに「MCを引き継いでいます…」。開けたらフラグを消し、「参加者とステージが自動でつながり直すのを待っています…」を誰かが戻るまで（最長30秒）表示。8回失敗したら新コードで作り直し、ヘッダに赤い帯で「部屋コードが変わりました：XXXX」を目立つように出す（×で閉じるまで残る）
- 前のMCの回答（'host' エントリ）は引き継がない（回答履歴に入っているものは残る）。新しいMCは自分の名前（自分のブラウザの `ogiri.hostName`）で 'host' エントリを新規に持つ。「自分も回答する」の設定は snapshot の `hostAnswers` を引き継ぐ
7. 他の参加者・ステージは何もしない。既存の自動再接続で新ホストにつながり、`clientId` 復帰で元のエントリ（フリップ・提出・挙手）に戻る

- 元MC（A）は参加フォームから入室すると参加者として入れる。同じブラウザの別タブで B が動いている場合は clientId が同じだが、B の旧エントリは除外済みなので新規参加者として入る
- 新しいMCは前のMCの「オープン時に自動でドン」・音量などの設定は引き継がない（自分のブラウザの設定のまま）

## 内蔵SE（Web Audio APIで合成、ファイル不要）

- ドン: 太鼓一発。低いサイン波（ピッチ下降）＋ノイズの短い減衰
- ドラムロール: ノイズ＋低音の連打を約2秒、最後に「ジャン」
- ジャン: 時間切れ・発表用の短いファンファーレ風（和音を短く）
- ピンポン: 2音の上昇（正解）
- ブザー: 低い矩形波を0.6秒（不正解）
- 拍手: ノイズバーストを不規則に1.5秒
- チーン: 高い正弦波を長めに減衰

AudioContextはユーザー操作（最初のクリック／タップ）で作成・resumeすること（モバイル対策）。

## 保存するもの

- localStorage: 名前、clientId、お題リスト、カスタムSEのメタ情報（id、名前）、音量、「オープン時に自動でドン」設定、MCの名前（`ogiri.hostName`）
- sessionStorage: ホストの部屋コード、ホストの状態（参加者・フリップ・stage・history など。リロード復元用）、MC交代用のフラグ（`ogiri.handoverPending`: 引き継いで起動中、`ogiri.handedOver`: 渡した相手の名前。トップ画面のお知らせ用）
- IndexedDB: カスタムSEの音声データ（Blob）

## ファイル構成（ビルド不要）

```
ogiri/
  index.html          … 全画面を含む1ページ（画面はJSで切り替え）
  css/style.css
  js/main.js          … 起点。URLパラメータ判定と画面切り替え
  js/net.js           … PeerJS接続（ホスト／クライアント両方）
  js/state.js         … ホストの状態管理（players, topic, timer…）
  js/flip.js          … フリップ描画（キャンバス描画・文字フィット）。参加者・MC・ステージで共通利用
  js/flipEditor.js    … フリップ編集（Pointer Events の手書き・undo・文字モード・送信タイミング）。参加者画面とMC画面「自分の回答」で共通利用。ツールバーは index.html の <template id="tpl-flip-tools"> から生成
  js/dialog.js        … ページ内の確認・通知ダイアログ（<dialog>）。アプリ内ブラウザ等で window.confirm/alert が使えない環境向け
  js/se.js            … 内蔵SE合成、カスタムSE（IndexedDB）、再生
  js/views/host.js    … MC画面
  js/views/player.js  … 参加者画面
  js/views/stage.js   … ステージ画面
  README.md           … 使い方（プログラミング未経験者向け）
  仕様.md             … この文書
```

## 制約・注意

- ビルドツール、npm install、フレームワークは使わない。`file://` ではなく http(s) で開く前提（README に書く）
- Chrome / Edge / iOS Safari / Android Chrome で動くこと
- 描画は `devicePixelRatio` を考慮し、スマホでもにじまないように
- 状態は必ずホストが正（クライアントは表示専用）。クライアントは自分の操作を送るだけで、画面は `state` を受けて描き直す（自分の手書き中のキャンバスだけはローカルで即時反映）
- 例外や切断でアプリが固まらないこと。コンソールにエラーが出ない状態で納品
